# REQUIREMENTS.md — 要件定義書

## 1. アプリ概要

登山の装備チェックリスト管理アプリ。

- 装備リスト(例:「夏山日帰り」「冬山テント泊」)を自由に作成・編集・削除できる
- 各リストに装備品を追加し、パッキング済みチェックを付けられる
- 初期状態で「夏山日帰り」「冬山日帰り」の2リストがシードデータとして投入されている
  (山岳救助経験に基づく実践的な内容にする)

## 2. データモデル

### GearList(装備リスト)

| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | Int | 主キー, autoincrement | |
| title | String | 必須 | リスト名(例: 沢登り) |
| description | String? | 任意 | 説明(例: 7月の日帰り想定) |
| createdAt | DateTime | 自動設定 | |
| updatedAt | DateTime | 自動更新 | |

### GearItem(装備品)

| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | Int | 主キー, autoincrement | |
| name | String | 必須 | 装備名(例: ツェルト) |
| quantity | Int | 必須, デフォルト1 | 数量(例: ペグ×8) |
| checked | Boolean | 必須, デフォルトfalse | パッキング済みフラグ |
| gearListId | Int | 外部キー | 所属リスト |
| createdAt | DateTime | 自動設定 | |
| updatedAt | DateTime | 自動更新 | |

### リレーション

- GearList 1 対 多 GearItem
- **onDelete: Cascade**(リスト削除時、所属アイテムも削除。孤児レコードを残さないため)

### 設計判断の記録(レビュー時に説明できるように)

- checkedをGearItem本体に持たせた理由: 1対1のCheckテーブル分離は、履歴やユーザー別状態が不要な今回は過剰設計。JOINと存在確認のコストだけ増える
- quantityを持たせた理由: ペグ・ガス缶など複数個持つ装備を「名前×数」で汚さず表現するため

## 3. API設計 (Hono)

ベースURL: `http://localhost:8787/api`

### GearList

| メソッド | パス | 説明 | 成功時 |
|---|---|---|---|
| GET | /lists | リスト一覧取得 | 200 |
| GET | /lists/:id | リスト詳細(所属アイテム含む) | 200 |
| POST | /lists | リスト作成 | 201 |
| PATCH | /lists/:id | リスト更新(title, description) | 200 |
| DELETE | /lists/:id | リスト削除(アイテムも連動削除) | 204 |

### GearItem

| メソッド | パス | 説明 | 成功時 |
|---|---|---|---|
| POST | /lists/:listId/items | アイテム追加 | 201 |
| PATCH | /items/:id | アイテム更新(name, quantity, checked) | 200 |
| DELETE | /items/:id | アイテム削除 | 204 |

### エラーレスポンス

- 400: バリデーションエラー(Zodで検証)
- 404: 対象リソースが存在しない
- 形式: `{ "error": "メッセージ" }`

## 4. 画面構成 (Next.js App Router)

| パス | 画面 | 主な機能 |
|---|---|---|
| / | リスト一覧 | 一覧表示、新規作成、削除 |
| /lists/[id] | リスト詳細 | アイテム一覧、追加、チェック切替、削除、リスト編集 |

※ フロントは最低限。会社の指示は「フロントよりAPIを重点的に」。

## 5. シードデータ

- 「夏山日帰り」: レインウェア、ヘッドランプ、ファーストエイドキット、行動食、地図・コンパス など
- 「冬山日帰り」: 上記 + アイゼン、ピッケル、ビーコン、ツェルト、テルモス など

## 6. スコープ外(やらないこと)

- 認証・ユーザー管理
- 画像アップロード
- デプロイ(ローカル動作でOK)

## 7. Stretch(完成後のバッファ期間のみ着手可)

1. リスト複製API: `POST /lists/:id/duplicate`
2. 装備の合計重量: GearItemにweightカラム追加 + Prismaのaggregateで集計
3. UI改善

---

# Phase 2 要件(2026-08-16 追記)

> **注記**: 上記「6. スコープ外」の「認証・ユーザー管理」は **Phase 1(入社前課題の必須要件)における**スコープ外指定である。
> Phase 1 は完了したため、認証は Phase 2 の要件として以下に定義し直す。
> 画像アップロード・デプロイは引き続きスコープ外。

## 8. 認証・認可(Priority 1)

方式は未決定。比較と推奨案は `docs/AUTH_DESIGN.md` を参照。

### 8-1. Authentication(認証)= 「誰であるか」

| # | 要件 |
|---|---|
| A1 | ユーザー登録ができる |
| A2 | ログインできる |
| A3 | ログアウトできる |
| A4 | ログイン状態が維持される(ページ遷移・リロードしても保たれる) |
| A5 | 現在ログインしているユーザーを取得できる |
| A6 | 認証が必要なAPIは、未ログインの場合 401 を返す |

### 8-2. Authorization(認可)= 「その操作をする権限があるか」

「ログインしている」ことは「その GearList を操作してよい」ことを意味しない。両者を明確に区別する。

| # | 要件 |
|---|---|
| Z1 | GearList は所有者(User)と関連付けられる |
| Z2 | 一覧には自分の GearList のみが表示される |
| Z3 | 他ユーザーの GearList を ID 直接指定で **閲覧** できない |
| Z4 | 他ユーザーの GearList を **更新** できない |
| Z5 | 他ユーザーの GearList を **削除** できない |
| Z6 | 他ユーザーの GearList に GearItem を **追加** できない |
| Z7 | 他ユーザーの GearItem を **更新・削除** できない(親リストの所有者で判定) |

> GearItem は User と直接の関連を持たず、**親 GearList の所有者を辿って**判定する方針(データモデルの正規化を保つため)。

### 8-3. セキュリティ要件

**今回の設計に関係するもの**(実装時に必ず確認):

| 項目 | 要件 |
|---|---|
| パスワード保存 | 平文保存しない。OWASP 推奨のハッシュ関数を使う(Argon2id 推奨、次点 scrypt。bcrypt はレガシー扱い・72バイト上限あり) |
| salt | ライブラリが自動生成するものを使う。自前実装しない |
| Cookie | `HttpOnly`(JSから読ませない=XSS対策)、`Secure`、`SameSite` を明示的に設定 |
| セッション | ID は推測不能な乱数(OWASP: 最低64bitのエントロピー)。**ログイン後にIDを再生成**(セッション固定化対策) |
| CORS | 現状 `origin: http://localhost:3000` に限定済み。Cookie を使う場合 `credentials` の設定が追加で必要 |
| エラーレスポンス | 「メールアドレスが存在しない」「パスワードが違う」を区別しない(ユーザー列挙攻撃の防止) |
| 不正アクセス | 他ユーザーの ID を直接指定した操作を必ず拒否する(8-2 の Z3〜Z7) |
| 秘密情報 | セッション署名鍵・DB接続情報は環境変数。`.env` は Git 管理外(既に `.gitignore` 済み) |

**今回の設計に関係が薄いもの**(用語として知っておくが、今回は対象外または優先度低):

| 項目 | 理由 |
|---|---|
| CSRF トークン | 現状の API は JSON のみ受け付ける。JSON の cross-origin POST は必ずプリフライトが飛ぶため CORS で防御される。`SameSite` も併用する。フォーム送信(`application/x-www-form-urlencoded`)を追加する場合に再検討 |
| JWT のトークン管理 | セッション方式を採用する場合は不要。JWT 方式を選ぶ場合のみ検討対象 |
| XSS | 直接の対策は React のエスケープに依存。Cookie を `HttpOnly` にすることで「XSSが起きてもセッションは盗まれない」多層防御を効かせる |

## 9. メール送信(発展候補・必須ではない)

> 位置づけ修正(2026-08-29): 8/17 面談で「認証の後に取り組める発展機能の一例」として口頭で挙がったもの。
> **必須課題ではない。** 何を送るか・どの技術/サービスを使うかは面談で指定されておらず、
> 着手する場合に相談して確定する。検討の叩き台は `TASKS.md` の Priority 2 を参照。

## 10. 非同期処理・Queue / Stack(時間があれば)

> 位置づけ修正(2026-08-29): 面談後チャットの「概念を調べて理解する、できそうな範囲で
> 作ってみるのもアリ」を受けたもの。**実装は必須ではなく、まず概念の理解が先。**
> AWS SQS は面談での具体例であり使用必須ではない。技術選定の観点は `TASKS.md` の Priority 3 を参照。

## 11. Phase 2 のスコープ外

- 画像アップロード
- デプロイ(ローカル動作でOK)
- OAuth / ソーシャルログイン(仕組みの理解を優先するため、まず自前実装)
- 多要素認証(MFA)
- パスワード変更・アカウント削除(必要になった時点で追加検討)
