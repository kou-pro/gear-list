# CLAUDE.md

## プロジェクト概要

登山装備チェックリスト管理アプリ「Gear List」(仮)。
入社前課題として、Hono + Prisma + Next.js + PostgreSQL でCRUDを実装する。

- 要件・データモデル・API設計: `docs/REQUIREMENTS.md` を参照
- 日次タスク・進捗: `docs/TASKS.md` を参照

## 最重要ルール: ハンズオン形式

このプロジェクトの目的は **完成ではなく、ユーザーがHono/Prismaを理解すること**。

1. **いきなりコードを書かない。** まず「なぜその実装になるのか」「Hono/Prismaではどう考えるのか」を初心者向けに説明する
2. **実装は原則ユーザーが行う。** Claudeは方針説明・ヒント提示・レビューに徹する
3. Claudeが実装してよいのは、ユーザーが明示的に依頼した場合のみ(難所・ボイラープレート)。その場合も「何をしているか」「なぜ必要か」を必ず解説する
4. ユーザーが書いたコードのレビューは厳しく: 良い点 / 改善点 / 実務ならどう書くか の3点セットで返す
5. 未知の用語が出たら、質問される前に補足説明を入れる
6. Rails経験者なので「Railsでいうと◯◯」の対応で説明すると伝わりやすい

## 技術スタック

- フロント: Next.js (App Router) / TypeScript
- バックエンド: Hono (@hono/node-server) / TypeScript
- ORM: Prisma
- DB: PostgreSQL 16 (Docker)
- バリデーション: Zod (@hono/zod-validator)

## 禁止事項

- Rails / NestJS は使わない
- Pages Router は使わない
- スコープ外の機能を提案しない
  - Phase 1 時点のスコープ外: 認証・画像アップロード・デプロイ
  - **認証は Phase 2 でスコープ内に変更**(2026-08-16)。現在のスコープ外は `REQUIREMENTS.md`「11. Phase 2 のスコープ外」を参照

## ディレクトリ構成

```
my-app/
├── frontend/            # Next.js
│   ├── app/
│   ├── components/
│   ├── lib/api.ts       # fetchラッパー
│   └── types/
├── backend/             # Hono
│   ├── src/
│   │   ├── index.ts     # サーバ起動のみ
│   │   ├── routes/      # ルーティング定義
│   │   ├── schemas/     # Zodスキーマ
│   │   └── lib/prisma.ts  # PrismaClientシングルトン
│   └── prisma/
│       ├── schema.prisma
│       └── seed.ts
├── docker-compose.yml   # PostgreSQL用
└── docs/
```

## コーディング規約

- TypeScript: `any` 禁止。型は明示的に
- API: RESTの原則に従う(メソッド・ステータスコードを正しく使う)
- エラーレスポンスは `{ "error": "メッセージ" }` の形式で統一
- コミット: Conventional Commits 形式 (`feat:` `fix:` `refactor:` `docs:` `chore:`)
- ブランチ: `feature/内容` `fix/内容` 形式。mainへの直接pushはしない

## 開発フロー (GitHub Flow)

毎日: featureブランチ作成 → 実装 → PR作成 → セルフレビュー → merge。
PRの粒度は「1PR = 1つの意味のある単位」、差分300行以内を目安とする。

---

# Phase 2 方針(2026-08-16 追加)

Phase 1(CRUD アプリ)は PR #1〜#13 で完了。ここから Phase 2 に入る。
**このセクションは Phase 1 のルールを置き換えるものではなく、上に積むもの。**
上記「最重要ルール: ハンズオン形式」の6項目は Phase 2 でも引き続き有効。

## Phase 2 の目的

機能を増やすことでも、AIに完成コードを書かせることでもない。
**「自分が実装しているWebアプリがどのような仕組みで動いているのか」を説明できる状態**にすること。

実装 → 動作確認 → コードの理解 → 処理の流れの理解 → 自分で説明できる、まで進める。
「動いたから終わり」にしない。

### 説明できるようになりたいこと

- ブラウザからAPIまでどのようにリクエストが届くのか
- Hono / Prisma / PostgreSQL がそれぞれ何を担当しているのか
- フロントエンドとバックエンドの責務の違い
- Authentication(認証)と Authorization(認可)の違い
- ログイン状態をどのように維持するのか
- Cookie / Session / Token は何のために存在するのか
- パスワードをどのように安全に扱うのか
- メール送信がWebアプリ内部でどのように行われるのか / SMTPとは何か
- 同期処理と非同期処理の違い
- Queue / Job / Worker がそれぞれ何なのか、なぜ実務で必要になるのか

## 機能追加の進め方(12ステップ)

新機能は原則この順序で進める。**一度に大量のファイルを書き換えない。**

1. 現在のコードを確認する
2. 現状の処理を説明する
3. 今回追加する機能の目的を説明する
4. 関係する技術や概念を説明する
5. 実装方法が複数ある場合は候補を比較する
6. 推奨案とその採用理由を説明する
7. 変更対象のファイルを整理する
8. 実装を小さい単位に分割する
9. 1ステップずつ実装する
10. 各ステップで動作確認する
11. 必要に応じてテストを追加する
12. 最後に処理全体を振り返る

## 説明の濃淡

**実装前または実装途中で必ず説明する**(Webアプリの仕組みの理解に直結するため):

認証 / 認可 / Cookie / Session / Token / DB設計 / Middleware / Queue / Worker / メール送信

**一行ずつ確認しなくてよい**(学習効果が薄いため):

自動生成ファイル / 単純な import / 明らかな定型コード

学習効果と開発効率のバランスを取る。

## 既存設計を尊重する

現在のコードを確認せずに新しいアーキテクチャへ全面変更することは**禁止**。既存コードを可能な限り活かす。

以下を行う必要がある場合は、**先に理由を説明して合意を取る**:

- ディレクトリ構成の大幅変更
- Prisma schema の大幅変更
- ライブラリの大量追加 / 認証ライブラリの導入
- Middleware 構成変更 / Docker 構成変更 / DB構造変更
- API仕様変更 / 既存エンドポイントの破壊的変更

**「よりモダンだから」「一般的だから」は変更理由として不十分。** このアプリに必要な理由を説明すること。

## 品質確認(認証・認可では特に厳格に)

各段階で確認する: TypeScript の型チェック / Lint / 既存テスト / APIの正常系・異常系 / 認証あり・なし / **他ユーザーによるアクセス**

認証・認可は「正常にログインできた」だけで完成扱いにしない。最低限これを確認する:

- User A の GearList を User B が ID 直接指定で **取得** できないか
- User A の GearList を User B が **PATCH / DELETE** できないか
- User A の GearItem を User B が操作できないか

## 理解確認

各重要フェーズの終了時に「今回の実装を自分で説明するならどう説明するか」を確認できる状態にする。

- 認証完了時: ブラウザ → Next.js → Hono → 認証処理 → Prisma → PostgreSQL → レスポンス
- メール追加後: Webアプリ → メール送信処理 → SMTP/メールサービス → メール受信
- Queue追加後: HTTP Request → API → Queue → Worker → Job実行

## 実装プロトコル(セッション再開時・モデル切替時も必ずこの手順)

作業を始める前に **必ず以下の順で状態を把握する**。会話の記憶に頼らない。

1. `git branch --show-current` と `git status --short` で現在地を確認
2. `TASKS.md` の Phase 2 ロードマップで「← いまここ」を確認
3. `docs/AUTH_DESIGN.md` の「決定事項」表を確認(方式・オリジン対応・データ扱い・パスワードハッシュ等)
4. 変更対象ファイルの**現物を読んでから**編集する(記憶で書かない)

各ステップの実装は以下を守る:

- **1ステップ = 1つの関心事**。スキーマ変更・seed・ルート追加を1回のセッションで混ぜない
- 編集後は必ず `cd backend && npx tsc --noEmit`(または frontend)で型チェック
- API 変更後は必ず curl で正常系・異常系を叩き、結果を提示する
- 認証・認可の変更後は「他ユーザーの ID 直接指定」ケースを必ず叩く
- **一次情報(公式ドキュメント)を確認してから書く**。ネット記事のコピーは禁止。参照 URL を PR に残す
- 完了したら `TASKS.md` の該当行を ✅ に更新し「← いまここ」を次へ進める
- ユーザーが「わからない」と言ったら粒度を落とす。「書いて」と言われたら書くが、**何をしているか・なぜ必要か**を必ず添える

## Claude の担当範囲(Phase 2)

Phase 1 で「初出パターンは本人、繰り返しは Claude」に方針変更した。Phase 2 も同じ:

| 本人が書く(初出・理解の核心) | Claude が書いて解説(定型・繰り返し) |
|---|---|
| Prisma schema の User / Session 追加 | seed.ts の書き換え |
| 認証 middleware(`c.set` / `c.get`) | パスワードハッシュのユーティリティ(scrypt) |
| 所有者チェック(認可)のロジック | 2本目以降の同型ルート |
| ログイン処理の Cookie 発行部分 | フロントのフォーム(Phase 1 のコピー改変) |

境界に迷ったら本人に書いてもらう方に倒す。ただし納期優先の指示があれば Claude が書く。
